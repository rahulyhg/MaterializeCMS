@extends('layouts.main')

@section('title', 'Home')

@section('content')
  {{-- @if (Route::has('login'))
    <div class="top-right links">
      @auth
        <a href="{{ url('/home') }}">Home</a>
      @else
        <a href="{{ route('login') }}">Login</a>
        <a href="{{ route('register') }}">Register</a>
      @endauth
    </div>
  @endif --}}
  <h1 style="margin-top: 10rem;">HAHAHA</h1>
@endsection